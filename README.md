# Synera — Auto Chess Arena

基于 **Qt6 + C++17 + MinGW** 的单机 PVE 自走棋对战游戏。玩家购买英雄、布阵，与敌方自动战斗，逐关推进。

---

## 基本信息

| 项目 | 内容 |
|------|------|
| 姓名 | 陈固沛 |
| 学号 | 251220107 |
| 项目阶段 | 全部完成（v0.5 - v0.14） |

### 各阶段完成度

| 阶段 | 版本 | 核心功能 | 完成度 |
|------|:----:|---------|:------:|
| 基础框架 | v0.5 | 三职业 + 自动战斗 + 拖拽布阵 | ✅ 100% |
| 技能系统 | v0.6 | 法力值 + 四职业技能多态 + 燃烧 debuff | ✅ 100% |
| 战斗经济 | v0.7 | 帧级计时器 + 5关 + 商店 + 回收槽 + 存档 | ✅ 100% |
| 刺客锁定 | v0.8 | 刺客锁定系统 + 伤害显示 + 回收槽扩容 | ✅ 100% |
| 升星人口 | v0.9 | 半星升星制 + 人口上限 + 利息 + 价格波动 | ✅ 100% |
| 招募重构 | v0.10 | 招募区5槽 + 英雄信息面板 + 回收双用途 | ✅ 100% |
| 羁绊系统 | v0.11 | 5种羁绊 + 刺客分身 + 分身继承buff | ✅ 100% |
| 整星装备 | v0.12 | 整星制改革 + 装备系统 + 移动优化 | ✅ 100% |
| 高级装备 | v0.13 | 高级装备合成 + 复活/反伤/防御 + 合成树窗口 | ✅ 100% |
| 战斗特效 | v0.14 | Boss 立绘 + 全职业普攻/技能打击特效 + 燃烧火焰 | ✅ 100% |
| 自定义难度 | v0.15 | 自定义对手编辑器独立窗口 + 框架优化 + 交互测试 | ✅ 100% |
| 全员立绘 | v0.16 | 四职业 + Boss 全部使用像素立绘替代色块 | ✅ 100% |
| 立绘全覆盖 | v0.17 | 商店/备战区/自定义窗口等全部展示位使用立绘 | ✅ 100% |
| 图标修复与节奏 | v0.18 | 修复装备图标 qrc 路径 + 放慢节奏让特效可见 | ✅ 100% |
| 模式与统计 | v0.19 | 开始界面三模式选择 + 无尽模式 + 战后统计面板 | ✅ 100% |
| 局域网联机 | v0.20 | 双机对战锁步同步 + 分模式存档 | ✅ 100% |

---

## 目录

- [基本信息](#基本信息)
- [快速开始](#快速开始)
- [操作方式](#操作方式)
- [游戏机制](#游戏机制)
  - [英雄类型](#英雄类型)
  - [升星机制](#升星机制)
  - [技能系统](#技能系统)
  - [装备系统](#装备系统)
  - [战斗系统](#战斗系统)
  - [羁绊系统](#羁绊系统)
  - [寻路索敌](#寻路索敌)
  - [刺客锁定机制](#刺客锁定机制)
  - [伤害显示](#伤害显示)
  - [战斗特效系统](#战斗特效系统)
- [经济系统](#经济系统)
- [招募系统](#招募系统)
- [关卡系统](#关卡系统)
  - [自定义难度系统](#自定义难度系统)
  - [游戏模式与开始界面](#游戏模式与开始界面)
  - [无尽模式](#无尽模式)
  - [战后统计面板](#战后统计面板)
- [回收系统](#回收系统)
- [存档系统](#存档系统)
- [代码架构](#代码架构)
- [AI 使用说明](#ai-使用说明)
- [更新日志](#更新日志)

---

## 快速开始

### 依赖

- Qt 6.x
- CMake 3.16+
- MinGW 64-bit (或 MSVC 2022)

### 构建

```bash
mkdir build && cd build
cmake .. -G "MinGW Makefiles"
mingw32-make
```

或使用 Visual Studio：

```bash
mkdir build && cd build
cmake .. -G "Visual Studio 17 2022"
cmake --build .
```

---

## 操作方式

| 操作 | 说明 |
|------|------|
| 鼠标点击招募槽 | 购买英雄（金币足够时英雄出现在回收槽，该槽变空） |
| 鼠标点击 Refresh 按钮 | 花费 15 金币刷新全部 5 个招募槽 |
| 鼠标点击 Pop+ 按钮 | 购买人口上限（招募区下方） |
| 鼠标拖拽 | 从回收槽拖拽英雄到棋盘（玩家半场 y≥4） |
| 拖拽到英雄信息面板 | 将英雄卖回（返还基础价格） |
| 拖拽到回收槽 | 将英雄存入回收槽备用 |
| 拖拽同整星同名叠加 | 升星合成（按整星匹配，非精确星等） |
| **自定义难度按钮** | 打开独立的对手编辑器窗口（左侧面板底部，紫色按钮） |
| 开始战斗按钮 | 点击右侧按钮开始本关战斗 |
| **R** | 返回开始界面重新选择模式 |
| **F5** | 保存游戏进度（准备阶段，仅通关模式） |
| **F9** | 读取游戏进度（准备阶段） |
| **R** | 完全重置游戏 |

---

## 游戏机制

### 英雄类型

| 类型 | HP | 攻击/治疗 | 射程 | 移速/攻速(帧) | 技能 |
|------|----|:---------:|:----:|:-------------:|------|
| 战士 Warrior | 100×(整星+1) | ATK 20×(整星+1) | 1 | 60/120 | 对最近敌方造成 80×(1+整星×0.5) 伤害 |
| 法师 Mage | 50×(整星+1) | ATK 10×(整星+1) | 4 | 60/120 | 周围 5×5 敌方全体燃烧 4 回合，每回合伤害 = 10 + 星数×5 |
| 辅助 Support | 80×(整星+1) | 治疗 20×(整星+1) | 2 | 60/120 | 全场 HP 最低 2 个单位各回复 30×(1+整星×0.5) |
| 刺客 Assassin | 15×(整星+1) | ATK 50×(整星+1) | 1 | 60/80 | 锁定距离≤2 的最近敌方造成 80×(1+整星×0.5) 伤害 |
| Boss | 500 | ATK 20 | 1 | 120/120 | 技能1: 全体英雄各 10 伤害（3法力）; 技能2: 米字路径全体英雄各 30 伤害（5法力） |

> 移速/攻速值为帧间隔（已全局翻倍以优化伤害显示），值越小行动越快。刺客攻速最快（80帧）。
> **整星** = starLevel，即英雄星等（0-3 星）。

### 升星机制

- 采用**整星制**：星等级 0-3 表示 0-3 颗完整星
- **3 个同名同星**英雄自动合成，结果为 `星等 + 1`（提高 1 颗星）
- 拖拽同名同星英雄叠加或在回收槽中满足 3 个相同英雄即可触发自动合成
- **属性加成整星生效**：属性公式使用 `整星 + 1`
- 刺客升星后初始法力值满格

### 技能系统

- 每次普攻/治疗积攒 **1 点法力值**，满蓝自动释放技能（需有有效目标）
- **无有效目标时不释放技能**，法力值保留至下回合
- 攻击技能（战士/法师/刺客/Boss）仅对敌方释放
- 辅助技能仅对友方释放
- 若有效目标数不足，则对现有有效目标使用；无有效目标则保留技能点

#### 技能目标验证

| 职业 | 有效目标条件 |
|------|------------|
| 战士 | 任意敌方存活 |
| 法师 | 技能范围内有敌方（5×5 以自身为中心） |
| 辅助 | 有受伤友方（HP < maxHP） |
| 刺客 | 曼哈顿距离 ≤ 2 内有敌方（由独立刺客系统处理） |

### 装备系统

英雄可装备最多 5 种类型的装备（每种 1 件），拖拽装备到英雄身上即可装备。

| 装备 | 类型 | 图标 | 效果 |
|------|:----:|:----:|------|
| Iron Sword | 攻击 | 剑 | 攻击力 +30 |
| Chain Mail | 防御 | 甲 | HP +150 |
| Speed Gloves | 攻速 | 攻速 | 攻速 ×0.8（帧间隔缩短 20%） |
| Blue Crystal | 法力 | 魔法石 | 技能消耗 ×0.5（减半） |
| Warhorse | 射程 | 战马 | 攻击范围 +1 |
| **Rune Greatsword** | 攻击 | 符文大剑 | 攻击力/治疗 +50，法力上限 -20 |
| **Swift Blade** | 攻击 | 极速战刀 | 攻击力 +30，攻速 ×0.5 |
| **Thorns Armor** | 防御 | 反伤铠甲 | HP +200，反弹 50% 所受伤害 |
| **Vitality Armor** | 防御 | 生命重甲 | HP +400，防御 +20（减伤），受治疗 ×1.5 |
| **Gale Gloves** | 攻速 | 疾风手套 | 攻速 ×0.7，法力上限 +20 |
| **Revive Stone** | 法力 | 复活石 | HP +100，法力回复 ×2，阵亡时消耗自身满血复活 |
| **Sniper Crossbow** | 射程 | 狙击弩 | 攻击力 +40，攻击范围 +2 |

> 高级装备（粗体）由基础装备合成获得，每次合成消耗 300 金币。

> v0.18 修复：装备图标曾因 qrc 虚拟路径带 `src/unit/` 层级而全部加载失败（显示为空框），
> 已为每项资源添加 `alias` 拍平路径，`:/equip/<文件名>` 现可正常加载。

#### 装备获取与使用

- **掉落机制**：战斗中每击杀一个敌方有概率掉落装备（概率 = `关卡 × 10% + 5%`），无每关掉落上限
- **装备存储**：掉落装备显示在回收槽下方，最多保留 10 件
- **拖拽装备**：从掉落区拖拽装备到棋盘或回收槽中的英雄身上即可装备
- **卸下装备**：点击英雄身上的装备图标即可卸下到掉落区
- **升星保留**：合成时3个源单位的所有装备自动汇集到升级英雄身上（每种类型保留1件，多余的放回掉落区）
- 装备效果在 `equip()` 时立即生效（HP/ATK/攻速/射程/法力消耗等）

#### 装备合成

- 将基础装备拖拽到已装备兼容基础武器的英雄身上，消耗 **300 金币**自动合成高级装备
- 合成配方：

| 高级装备 | 合成材料 |
|---------|---------|
| 符文大剑 | 魔法石 + 剑 |
| 极速战刀 | 攻速 + 剑 |
| 反伤铠甲 | 甲 + 剑 |
| 生命重甲 | 甲 + 甲（第二个从掉落区拖入） |
| 疾风手套 | 攻速 + 魔法石 |
| 复活石 | 魔法石 + 甲 |
| 狙击弩 | 战马 + 剑 |

- 装备栏下方显示提示："每次升级装备需要耗费300金币"

#### 装备合成树窗口

- 点击左侧面板「装备合成树」按钮打开独立的合成参考窗口
- **非模态窗口**：不影响主窗口操作，可同时查看合成配方和进行游戏
- **独立渲染**：拥有自己的 `paintEvent`，与主窗口完全解耦
- **生命周期**：关闭窗口仅隐藏不销毁，下次点击按钮直接显示；程序退出时释放
- 窗口展示所有高级装备的合成配方、材料、效果和花费，按类型分色显示

### 战斗系统

- **帧级独立计时器**：每个单位有独立的 `m_moveTimer` 和 `m_attackTimer`
- 每帧计时器 +1，达到阈值时执行对应行动
- 移动计时器和攻击计时器完全独立，可同一帧同时触发
- 攻击范围内无目标时攻击计时器**每帧重置**，确保靠近后需等待完整攻速间隔

#### 战斗流程（每帧）

```
1. 清理过期特效
2. 收集棋盘存活单位
3. 燃烧结算（每 60 帧一次）
4. 刺客技能锁定处理（燃烧结算后立即执行）
5. 遍历所有单位：
   - 满蓝且非刺客 → 验证目标 → 释放技能或保留
   - 刺客 → 跳过主循环技能（由刺客系统处理）
   - 移动 & 普攻/治疗
6. 执行移动
7. 刷新伤害显示事件
8. 判定胜负
```

#### 燃烧 debuff

- 法师技能施加，每回合扣除 `10 + 星数 × 5` HP，持续 4 回合
- 燃烧伤害由施法者星数决定，0星=10/回合，3星=25/回合
- 独立于施法者生命周期
- 每 60 帧结算一次（燃烧间隔已翻倍）

### 羁绊系统

当场上满足特定条件时触发羁绊效果，在**每帧战斗开始时**自动判定（激活/失效）。

| 羁绊 | 条件 | 效果 |
|------|:----:|------|
| 战斗不息 | 3+ 战士 | 所有战士 HP 翻倍 |
| 吟咏魔典 | 2+ 法师 | 共享技能点（法师回蓝时平分给其他法师） |
| 生生不息 | 2+ 辅助 | 所有辅助治疗翻倍 + 治疗范围 +1 |
| 暗夜幻影 | 2+ 刺客 | 每个刺客生成 1 个分身（星数 = max(0, 本体星数 - 1)） |
| 全军出击 | 4 职各 ≥1 | 全体英雄 HP×1.5、ATK+20、技能消耗 -20 |

#### 羁绊触发规则

- **羁绊仅在有单位变化时重新判定**，避免每帧重复计算
- 羁绊激活时立即生效，失效时立即移除效果
- 准备阶段显示羁绊激活预判（仅预览，不实际生效）

#### 刺客分身机制

暗夜幻影羁绊激活时，每个刺客本体生成 1 个分身，特殊规则如下：

| 属性 | 规则 |
|------|------|
| **星数** | `max(0, 本体星等级 - 1)`，即比本体低 1 星，最低 0 星 |
| **初始法力** | 始终从 0 开始 |
| **本体死亡** | 分身不受影响，可继续战斗 |
| **关卡结束** | 所有分身自动消失（直接移除） |
| **人口计数** | 分身不影响场上角色总人数（不计入人口上限） |
| **羁绊享受** | 分身可享受全军出击等全体 buff |
| **羁绊触发** | 分身可参与全军出击等羁绊的条件判定 |

### 寻路索敌

- **索敌规则**：欧几里得距离最近优先，距离相等时优先横向距离更近的
- **移动规则**：优先前进（纵向靠近目标），其次左右（横向），避免双方错位时镜像循环卡死
- **辅助移动**：优先向右，其次纵向，最后向左
- 攻击性和辅助性角色使用相同索敌规则

### 刺客锁定机制

刺客技能**仅在燃烧结算帧处理**（每 60 帧一次），不通过主循环释放。

**锁定规则**：

1. 收集所有满蓝且范围内有敌方的刺客
2. 按棋盘顺序依次处理每位刺客：
   - 索敌（欧氏距离最近、横向破平，范围 ≤2）
   - **互杀检测**：若目标也是满蓝刺客 → 双方同归于尽，统计金币
   - **重复锁定**：若目标已被前序刺客击杀 → 自动重新索敌
   - 重索失败 → 本回合不释放技能，保留法力值
3. 对目标造成技能伤害并消耗法力值

### 伤害显示

- 每次攻击/治疗/技能产生 HP 变化后，在角色**右上角**显示浮动文本
- 格式：`+X`（绿色，治疗）或 `-X`（红色，伤害）
- 显示时长 = `max(最快攻速帧数 / 该单位本帧事件数 - 3, 0)` 帧
- 每帧结束后统一刷新，回合结束自动清空，不留残影

### 战斗特效系统

v0.14 为全部职业补充了普攻与技能的打击/受击视觉反馈，所有特效均为**纯视觉层**，不影响战斗逻辑数值。特效容器（斩击/弹道/治疗/残影）与 `m_frameCounter` 帧计数联动，在 `processCombatFrame` 开头统一做过期清理。

| 职业 | 普攻特效 | 技能特效 |
|------|---------|---------|
| 战士 Warrior | 攻击者与目标之间出现**刀光弧 + 命中闪光 + 火花溅射**（交替挥砍方向，持续 22 帧） | **交叉重斩**：对侧双弧线、更大光晕、8 道火花并追加命中冲击环（持续 30 帧） |
| 法师 Mage | **小火球弹道**：从法师沿弧线飞向目标（`18 + 6×距离` 帧，4 格射程约 0.9 秒），带拖尾火点，命中后橙色冲击环 + 碎火星爆裂 | 施加燃烧后目标身上**持续冒红色火苗**（见下） |
| 辅助 Support | 被治疗者身上冒出**绿色 "+"**，上浮淡出并伴随 2 个小 "+" 飘散 | **大 "+"（22px）+ 3 个小 "+"** 粒子群，所有被治疗友方同时显示 |
| 刺客 Assassin | **快速斩击**（16 帧，更短促锋锐的金白色刀光） | 瞬移到目标身旁时留下**高透明度残影**：路径 25%/50%/75% 处 3 个幻影 + 虚线轨迹，整体快速淡出 |
| Boss | —（普攻特效同近战逻辑，暂不触发斩击） | — |

#### 燃烧火焰特效

- 身处燃烧 debuff 中的单位身上**持续冒红色小火苗**（径向渐变：内核亮黄 → 外焰红橙）
- 3 个火苗循环上浮，各自带伪随机水平摆动，先亮后暗
- 动画由 `m_frameCounter` 驱动，无需额外的特效结构体

#### 单位立绘

- **全部职业 + Boss 均使用像素立绘替代色块**（v0.16 起），映射表见 `loadUnitPortraits()`：

| 职业 | 立绘文件 | 形象 |
|------|---------|------|
| 战士 Warrior | `World01_007_Pirate.png` | 持械海盗 |
| 法师 Mage | `World01_006_Witch.png` | 女巫 |
| 辅助 Support | `World01_001_GreenGoo.png` | 绿色黏液 |
| 刺客 Assassin | `World01_003_Bird.png` | 敏捷飞禽 |
| Boss | `World01_004_WailingPrince.png` | 哀恸王子 |

- 渲染：暗色底框 + 阵营描边（英雄蓝 / 敌方红 / Boss 金色加粗）+ 平滑缩放贴图；名字/HP 条/法力条/装备框/星数仍叠加在立绘之上
- **立绘覆盖全部展示位**：棋盘、拖拽幽灵、英雄信息面板、招募区、回收槽（备战区）、自定义难度窗口类型按钮，统一走 `unitvisuals.h` 的 `unitPortrait()` 惰性缓存 + `drawUnitChip()` 通用图块
- 任一展示位立绘缺失自动回退原色块 + 单字标签，不影响运行
- 任一立绘加载失败自动回退原色块渲染，不影响运行
- 加载时依次回退尝试 `src/unit/` → `../src/unit/` → exe 目录相对路径，兼容任意工作目录
- CMake 配置阶段自动把 `src/unit/` 图片拷贝到构建目录，保证 `build/Synera.exe` 直接运行也能加载

#### 特效实现要点

- 斩击方向随 `startFrame` 奇偶**交替翻转**，连续攻击时左右挥砍更自然
- 所有粒子（火花/小"+"/碎火星/火苗摆动）使用**确定性伪随机**（以格子坐标 + 起始帧为种子），同一特效在存活期内的每一帧渲染稳定、无闪烁
- 特效坐标基于**格子坐标**而非单位指针，单位在特效期间死亡/移动也不会产生悬挂引用

---

## 经济系统

### 金币获取

| 来源 | 金额 |
|------|------|
| 初始金币 | 1360 |
| 每关基础通关 | 100 |
| 击杀敌方 Warrior | 80 × (整星+1) |
| 击杀敌方 Mage | 60 × (整星+1) |
| 击杀敌方 Support | 30 × (整星+1) |
| 击杀敌方 Assassin | 30 × (整星+1) |
| 击杀 Boss | 200 × (整星+1) |
| 第 5 关前补助 | 800 |

- 玩家获胜：`m_gold += m_pendingGold + 100`
- 玩家失败（HP > 0）：`m_gold += m_pendingGold / 2 + 100`

### 金币利息

- 每关胜利后、添加战斗奖励前，**未使用金币变为 1.5 倍**
- 仅在**有下一关**时生效（即第 1-4 关胜利后）
- 失败或最终关不发放利息

### 英雄价格（波动制）

| 职业 | 基础价格 | 波动范围 |
|------|:----:|:----:|
| Warrior | 100 | 80 - 120 |
| Mage | 80 | 60 - 100 |
| Support | 50 | 30 - 70 |
| Assassin | 60 | 40 - 80 |

- 价格公式：`基础价格 + 5 × (rand()%5) × ((rand()%3) - 1)`
- 波动范围：基础价格 ±20（步长 5），最低 ≥ 1
- 价格在**每关准备阶段开始时**和**刷新招募区时**随机生成
- 招募区**实时显示当前波动价格**

---

## 人口上限

- 场上玩家方角色总数不能超过**人口上限**
- **初始人口上限**：4
- **升级费用**：`100 × (当前上限 - 4)` 金币（首次升级免费）
- **UI 显示**：棋盘右上角显示 `Pop: 当前人口/人口上限`
- **购买按钮**：左侧招募区下方「Pop+ $N」按钮
- 人口上限**跨关卡保留**（存档/读档支持）

---

## 招募系统

- 左侧面板分为**英雄信息区**（只读）和**招募区**（可交互）
- **英雄信息面板**（4 个）：显示每种英雄的基础属性（HP/ATK/Heal/射程/移速/攻速/价格），**拖拽英雄到对应面板可卖回**返还基础价格
- **招募区**（5 槽）：每槽随机显示英雄类型 + 波动价格，**点击直接购买**
- 购买后英雄**自动放入回收槽**空位（无空位则无法购买），该招募槽变空
- **刷新按钮**（Refresh $15）：花费 15 金币重随全部 5 个招募槽
- 招募区在每关准备阶段开始时自动刷新

---

## 关卡系统

| 关卡 | 敌方配置 | 总计 |
|:----:|---------|:----:|
| 1 | 每种类型各 1 个（0 星） | 4 |
| 2 | 每种类型各 2 个（0 星） | 8 |
| 3 | 每种类型各 3 个（0 星） | 12 |
| 4 | 每种类型各 2 个（2 星） | 8 |
| 5 | 每种类型各 1 个（3 星）+ Boss | 5 |

- 仅一方**全部阵亡**时战斗结束
- 玩家 HP 初始 100，失败扣除 `剩余敌人数 × 20`
- HP ≤ 0 → **GAME OVER**
- 通过全部 5 关 → **VICTORY**

---

## 自定义难度系统

v0.15 新增的独立对战沙盒：玩家自由配置敌方编成进行战斗，**完全独立于 5 关关卡进度**。

### 入口与窗口

- 主界面左侧面板底部紫色「自定义难度」按钮（装备合成树按钮下方）打开独立窗口
- 与装备合成树同模式：**非模态、独立渲染层（自绘 paintEvent）、关闭仅隐藏不销毁**

### 对手配置

- 最多同时配置 **8 组**敌方，每组可调：
  - **角色类型**：点击循环切换 战士 → 法师 → 辅助 → 刺客 → Boss → 战士
  - **等级**：点击循环 0-3 星（Boss 属性固定，星级锁定为 —）
  - **数量**：+/- 调整该组数量
  - **删除**：移除该组；「+ 添加一组敌方」新增（上限 8 组）
- 「清空重置」恢复默认（1 个 0 星战士）

### 数量校验（双重）

| 层级 | 规则 |
|------|------|
| 窗口侧 | 所有组数量总和 **≤ 32**（敌方半场 8×4 格容量上限），+ 按钮到顶后自动锁定；总数为 0 时「开始」按钮置灰 |
| 主窗口侧 | `startCustomBattle()` 收到配置后**再校验**一次（字段范围 + 总量上限 + 至少 1 名上场英雄 + 处于准备阶段），不信任跨窗口数据 |

### 战斗结算规则（与关卡战斗的差异）

| 项目 | 关卡战斗 | 自定义战斗 |
|------|---------|-----------|
| 击杀金币 | 全额（胜）/ 半额（败） | 相同 |
| 通关 +100 / 利息 ×1.5 | 有 | **无** |
| 失败扣玩家 HP | 剩余敌数 ×20 | **不扣** |
| 关卡推进 / GAME OVER | 有 | **无**（战斗结束回到当前关准备阶段） |
| 存活英雄回收 | 回收槽 + 治疗 20 | 相同 |

- 自定义战斗进行中，顶部关卡标题显示 `Level X / 5 (自定义)`
- 羁绊、装备、特效系统在自定义战斗中全部照常生效

### 自动化验证

- 主程序带验证钩子：设置环境变量 `SYNERA_SHOW_CUSTOM=1` 启动即自动打开自定义难度窗口
- `tests/test_custombattle.cpp`：用**合成鼠标事件**驱动窗口的 headless 交互测试（18 项断言），覆盖数量上限锁定、类型/星级循环、Boss 星级锁定、删除/添加与合法性判定；编译运行方式见文件头注释

## 游戏模式与开始界面

v0.19 起启动游戏先进入**开始界面**（独立自绘窗口）选择模式，主游戏页面三模式复用：

| 模式 | 说明 | 结算规则 |
|------|------|---------|
| 通关模式 | 原 5 关战役 | 原有规则（HP/利息/通关奖励/存档 F5F9） |
| 无尽模式 | 波次无限 | 见下节 |
| 自定义模式 | 沙盒 | 战斗只能从「自定义难度」窗口发起，主界面开始按钮置灰提示 |
| 联机对战 | 局域网双人对战 | 双端锁步同步战斗，多回合计分，见下节 |

- 游戏中按 **R** 返回开始界面重新选模式
- 开始界面显示无尽模式历史最佳波数（存于 `endless_best.txt`）
- 存档（F5/F9）仅通关模式可用

## 无尽模式

每波击败敌方编成后进入下一波，编成按以下规则**递进演化**：

1. **增员**：每通关一波，随机某职业 +1 个 0 星怪物
2. **升星**：怪物总数达到战场容量上限（32）后不再增员，改为**随机一个最低星怪物升 1 星**（全体同星时等价于随机升任一）
3. **全体强化**：所有怪物都到 3 星满星后，每再通关一波，**全体怪物攻击/生命 +1%**，无限叠加

- 初始编成：四职业各 1 个 0 星
- 每波奖励：击杀金币全额 + 100 基础奖励（无尽无利息）
- 失败即结束：显示"共坚持 N 波"，刷新最佳纪录
- 强化实现：复用羁绊加成通道（`applyBondHpMult` / `applyBondAtkBonus`），每波按编成+强化重新生成怪物

## 战后统计面板

每场战斗结束（三种模式通用）自动弹出**战斗统计**独立窗口：

| 列 | 说明 |
|----|------|
| 英雄 | 像素立绘小图块 + 名字 + 星数（★★★） |
| 输出伤害 | 普攻 + 技能伤害，按此列降序排列 |
| 承受伤害 | 全部来源（普攻/技能/燃烧/反伤，经防御减免后） |
| 治疗 | 普攻治疗 + 技能治疗 |
| 击杀 | 普攻击杀 + 技能击杀 |

- 阵亡英雄也计入（快照在结算清理前收集，按输出排序）
- 底部合计行汇总；标题按模式显示（`Level X 胜利` / `无尽模式 - 第 N 波 胜利` / `自定义战斗 - 胜负`）
- 已知边界：**燃烧持续伤害未计入个人输出**（燃烧结算不携带施法者信息）
- 后台弹窗修复：新窗口以 `showNormal` 强制还原置前，避免被 Windows 最小化到任务栏

## 联机对战系统

两台电脑连同一局域网即可对战（同一台电脑也可用 127.0.0.1 自测）。

### 大厅与连接

- 开始界面选「联机对战」→ 打开联机大厅：一方**创建房间**（自动显示本机局域网 IP + 端口 45454），另一方**输入对方 IP 加入**
- 连接成功后双方进入联机准备阶段：各获 **3000 金币**预算、人口上限 10，正常购买/布阵/装备（双方商店各自随机，预算相同保证公平）
- 点击「**准备对战**」锁定并发送己方阵容；双方就绪后自动开战（主机生成随机种子随 START 消息下发）

### 锁步同步（保证双端展示完全一致）

联机对战不采用"主机算完再转发录像"的方案，而是更实时的**锁步确定性同步**，双端展示逐帧一致：

| 保障 | 实现 |
|------|------|
| 初始状态相同 | 双端执行同一份重建代码：**主机阵容 = Hero 侧原坐标、客户端阵容 = 180° 镜像敌方侧**，从阵容快照重建棋盘 |
| 随机数相同 | 主机生成种子随 START 下发，双端 `std::srand(同种子)`；战斗期间禁用一切其它随机调用（联机不掉装备） |
| 逻辑与时间解耦 | 战斗为纯帧驱动（QTimer 每帧 +1 逻辑帧），无实时时钟参与结算，两台机器快慢不影响结果 |
| 同一可执行 | 双方运行同一份编译产物，浮点/容器行为一致 |
| 权威结果 | 主机结算后补发 RESULT，供对拍校验（正常与本地锁步结果一致） |

- 视角说明：双端看到的棋盘**完全相同**（主机阵容在下方）——这正是"镜像内容保持一致"的实现方式
- 战后自动弹出统计面板（Hero 侧 = 主机阵容），比分 `我方:对方` 显示在顶部，胜方 +1
- 每回合结束自动回到准备阶段重买重布（比分与连接保留），随时可再战下一局
- 断线处理：对方断开时状态栏提示「联机未连接」，重连需按 R 回开始界面重建房间

### 联机协议（TCP，换行分隔的紧凑 JSON）

| 消息 | 方向 | 内容 |
|------|------|------|
| `lineup` | 双向 | 己方阵容快照（类型/星级/坐标/HP/法力/装备名） |
| `start` | 主机→客户端 | 随机种子 |
| `result` | 主机→客户端 | 权威胜负（对拍校验用） |

## 存档系统（分模式）

- **每个模式独立一个档位**，同时最多各存一份，新存档自动覆盖旧档（截断式写入）：
  - 通关模式 → `savegame_campaign.json`
  - 无尽模式 → `savegame_endless.json`（额外保存波次/全体强化/编成）
  - 自定义模式 → `savegame_custom.json`
  - 联机对战 → 不提供存档（对战为回合制即时进行）
- 兼容旧档：通关模式无新档时自动回退读取 legacy `savegame.json`
- F5 保存 / F9 读取（准备阶段），读取按当前模式读取对应档位

## 回收系统

- 回收槽：**2 行 × 8 列 = 16 槽**（与棋盘宽度一致）
- **双重用途**：① 战后回收英雄暂存 ② **从招募区购买英雄的暂存空间**
- 购买英雄后自动放入空回收槽（无空位则无法购买）
- 关卡胜利后自动回收场上存活英雄（治疗 20 HP，保留技能点）
- **回收优先级**：星数高优先 → 同星按 战士 > 法师 > 辅助 > 刺客
- 回收槽满时，剩余英雄丢弃
- 准备阶段可从回收槽拖拽英雄到棋盘或放回
- 回收槽常驻显示（空槽半透明边框，有单位则实心色块）
- 支持回收槽内升星合成

---

## 存档系统

- **F5** 保存（准备阶段），**F9** 读取（准备阶段）
- 存档文件：**按模式分档**（详见[存档系统（分模式）](#存档系统分模式)），通关模式兼容旧版单档 `savegame.json`
- 已有存档时**自动覆盖**（先删旧档再建新档）
- 存档内容：金币、关卡、HP、人口上限、招募区（类型/价格/空位）、棋盘所有英雄（位置/HP/法力/星等/装备/燃烧）、回收槽所有英雄

---

## 代码架构

### 文件树

```text
PA/
├── CMakeLists.txt
├── README.md
├── main.cpp                   # 入口
├── synera.h / synera.cpp      # 主窗口、渲染、游戏循环、战斗、关卡/经济/回收
├── unit.h / unit.cpp          # Unit 基类、UnitType 枚举、速度/计时器、坐标/距离
├── hero.h / hero.cpp          # Hero + 四种职业英雄子类
├── enemy.h / enemy.cpp        # Enemy + 四种职业敌方子类 + Boss
├── weapon.h / weapon.cpp      # 装备类（基础 + 高级合成装备）
├── board.h / board.cpp        # 8×8 棋盘数据
├── equipsynthwindow.h / equipsynthwindow.cpp  # 装备合成树独立窗口
├── custombattlewindow.h / custombattlewindow.cpp  # 自定义难度独立窗口（对手编辑器）
├── unitvisuals.h              # 共享的职业配色/标签定义（主窗口与独立窗口共用）
├── tests/test_custombattle.cpp # 自定义难度窗口 headless 交互测试
└── src/unit/*.png             # 单位图片资源（Boss 立绘 World01_004_WailingPrince 等）
```

### 类层次

```
Unit (abstract)
├── m_type: UnitType {Warrior, Mage, Support, Assassin, Boss}
├── m_mana / m_mana2 / m_burning
├── m_moveSpeed / m_attackSpeed / m_moveTimer / m_attackTimer
├── getAttackRange() / getAttackDamage() / getHealAmount() / canHeal()
├── useSkill(board, allUnits) = 0    [纯虚：职业技能]
├── useSkill2(board, allUnits)       [虚：Boss进阶技能]
├── applyBurning() / tickBurning()
│
├── Hero ──── WarriorHero / MageHero / SupportHero / AssassinHero
└── Enemy ─── WarriorEnemy / MageEnemy / SupportEnemy / AssassinEnemy / BossEnemy

Board (8×8)  ◄── Unit*[8][8]

Synera (QMainWindow)
├── Board + vector<unique_ptr<Unit>> + vector<RecruitSlot> (招募区)
├── m_recycleSlots[16] (2行×8列回收槽 / 购买暂存)
├── m_equipDrops / m_pendingEquip / m_weapons (装备系统)
├── m_hitEffects / m_slashEffects (战斗特效)
├── m_pendingDamageEvents (伤害累积显示)
├── m_bondActive[5] (羁绊激活状态)
├── 渲染: renderBoard / renderUnits / renderHeroInfo / renderRecruitment / renderRecycleSlots / renderEquipDrops / renderUI / renderBonds
├── 战斗: processCombatFrame / processBurningTick / processAssassinSkills
├── 羁绊: checkAndApplyBonds / applyBondEffect / revertBondEffect / spawnAssassinClones / removeAssassinClones
├── 索敌: findNearestEnemyFor / findHealTarget / findNearestAlly
├── 结算: checkLevelEnd / endLevel / initLevel
└── 存档: saveGame / loadGame
```

### 关键枚举

```cpp
enum class UnitType  { Warrior, Mage, Support, Assassin, Boss };
enum class EquipType { Attack, Defense, Speed, Mana, Range, COUNT };
enum class GamePhase { Preparation, Battle };
```

### 辅助函数

项目中定义了一些全局工具函数，用于坐标计算、距离判定等通用操作：

| 函数 | 所在文件 | 作用 |
|------|---------|------|
| `hexCellSize()` | `synera.h` | 返回六边形格子的像素尺寸，用于统一缩放棋盘 |
| `gridToPixel(int col, int row)` | `synera.cpp` | 将棋盘网格坐标转换为屏幕像素坐标（含偶数行偏移） |
| `pixelToGrid(QPointF pos)` | `synera.cpp` | 将屏幕像素坐标转换回棋盘网格坐标（反向查找最近格子） |
| `manhattanDistance(Unit*, Unit*)` | `unit.h` | 计算两个单位之间的曼哈顿距离（|dx| + |dy|） |
| `euclideanDistance(Unit*, Unit*)` | `unit.h` | 计算两个单位之间的欧几里得距离（√(dx²+dy²)） |
| `isAlly(Unit*, Unit*)` | `unit.h` | 判断两个单位是否属于同一阵营（玩家方/敌方） |
| `clamp(int val, int lo, int hi)` | `synera.cpp` | 将值限制在 `[lo, hi]` 范围内，用于边界保护 |

---

## AI 使用说明

本项目在开发过程中充分利用 AI 辅助编程，但所有代码均经过人工审查与理解，确保对每一行代码拥有完全掌控。

### 项目规划

项目采用**渐进式迭代**的开发策略，将整体拆分为 9 个独立阶段（v0.5 - v0.13），每个阶段聚焦一个核心子系统：

1. **分层解耦**：将游戏拆分为渲染层（GUI）、逻辑层（战斗/经济/羁绊）、数据层（Board/Unit），各层通过明确接口交互
2. **逐模块交付**：每阶段向 AI 提供明确的功能规格（输入/输出/边界条件），由 AI 生成初始实现，人工审查后集成
3. **增量验证**：每完成一个阶段立即进行功能测试，确认无误后再进入下一阶段，避免错误累积
4. **重构预留**：在早期阶段（v0.5-v0.7）有意保留扩展点（如虚函数、枚举），便于后续阶段在不破坏现有代码的前提下添加新功能

AI 在以下环节提供了高效辅助：
- 模板代码生成（类声明、getter/setter、信号槽连接）
- 算法草稿（寻路启发式、排序比较器、状态机转换）
- UI 布局计算（坐标变换、几何绘制、颜色混合）
- 调试辅助（日志插桩、边界条件检查、内存泄漏排查）

### 核心代码解析

#### 1. 寻路与索敌系统

**运作原理**：每个单位在每帧战斗中独立决定移动方向，核心步骤如下：

1. **目标选择（索敌）**：遍历棋盘上所有敌对单位，计算欧几里得距离 `√((x₁-x₂)² + (y₁-y₂)²)`，选择距离最近的目标。若多个目标距离相等，优先选择横向距离（|dx|）更近的那个作为最终目标，避免在两个等距目标间反复切换。

2. **方向计算**：获得目标位置后，比较当前坐标与目标坐标：
   - 纵向偏移 |dy| > 0 时，优先纵向移动（攻击单位向目标靠近，辅助单位优先向右平移再纵向移动）
   - 纵向已对齐（|dy| = 0）时，再横向移动靠近目标
   - 这种"先纵后横"的设计避免了双方在同一行时出现镜像循环卡死的问题

3. **目标位置校验**：移动前检查目标格是否为空且合法（棋盘范围内）。若目标格被占用，尝试侧向绕行；若所有相邻格均被占用，本帧不移动。攻击范围内已有敌方时停止移动，直接进入攻击阶段。

4. **辅助单位特殊处理**：辅助英雄没有攻击目标，其"索敌"实际上是寻找 HP < maxHP 的受伤友方。移动方向上辅助单位优先向右探索，其次纵向，最后向左，确保辅助单位倾向于在战场中散布而非聚集。

#### 2. 每回合判定过程（含刺客技能判定）

**运作原理**：战斗的每一帧按以下严格顺序执行判定，确保各系统间的依赖关系正确：

1. **回合初始化**：清理上一帧的过期伤害特效（`m_hitEffects` / `m_slashEffects`），收集棋盘上所有存活单位到临时列表。若有单位在本帧之前死亡（HP ≤ 0），将其从棋盘移除并释放内存。

2. **羁绊判定**：仅在战场单位数量发生变化时（有单位死亡或新单位入场），重新统计各职业在场数量。依次检查 5 种羁绊的激活条件：
   - 若条件满足且该羁绊此前未激活 → 应用羁绊效果（HP翻倍/共享技能点/治疗翻倍/生成分身/全体buff）
   - 若条件不再满足且该羁绊此前已激活 → 移除羁绊效果（恢复原始属性）
   - 分身不计入人口统计但可参与羁绊条件判定

3. **燃烧结算**（每 60 帧一次）：遍历所有单位，对带有燃烧标记（`m_burning > 0`）的单位扣除 `10 + 施法者星数 × 5` 点 HP，燃烧剩余回合 -1。若单位因燃烧死亡，标记待清理。

4. **刺客技能判定**（燃烧结算后立即执行，每 60 帧一次）：
   - 收集所有满足条件的刺客：① 存活 ② 法力值满（`m_mana >= m_mana2`）③ 曼哈顿距离 ≤ 2 范围内存在敌方
   - 按棋盘位置顺序依次处理每位刺客：
     - 索敌选择距离 ≤ 2 内的最近敌方（欧氏距离优先，横向破平）
     - **互杀检测**：检查目标是否也是满蓝刺客。若是，双方立即同归于尽（HP 归零），赏金计入 `m_pendingGold`
     - **重复锁定检测**：检查目标是否已被本轮前序刺客击杀。若已被杀，自动重新索敌；若重索失败（范围内无敌方），本回合保留法力值不释放技能
   - 对通过所有检测的刺客，对目标造成 `80 × (1 + 整星 × 0.5)` 点技能伤害，消耗全部法力值

5. **主循环处理**（遍历所有存活单位）：
   - 非刺客单位满蓝时 → 验证技能有效目标（战士需任意敌方存活，法师需5×5范围内有敌方，辅助需有受伤友方）→ 有目标则释放技能，无目标则保留法力值
   - 刺客在燃烧帧由刺客系统处理，主循环中跳过其技能释放
   - 所有单位执行移动（向索敌目标靠近 1 格）和普攻/治疗（攻击范围内有目标时造成伤害或治疗）

6. **伤害显示刷新**：收集本帧所有 HP 变化事件，计算每个事件的显示时长（`max(最快攻速帧数 / 该单位本帧事件数 - 3, 0)`），在单位右上角绘制浮动文本。

7. **胜负判定**：检查双方存活单位数量。若玩家方全部阵亡 → 玩家失败；若敌方全部阵亡 → 玩家胜利。触发对应的关卡结束流程。

#### 3. 装备合成树窗口

**运作原理**：`EquipSynthWindow` 是一个完全独立于主窗口渲染管线的非模态子窗口，生命周期由主窗口 `Synera` 手动管理，核心设计要点如下：

1. **独立窗口创建**：构造时传入 `Qt::Window` 标志使其成为独立顶层窗口，设置 `WA_DeleteOnClose = false`（关闭不销毁）和 `WA_QuitOnClose = false`（关闭不退出程序）。窗口大小为 560×620 固定尺寸。

2. **关闭拦截**：重写 `closeEvent()`，调用 `event->ignore()` 阻止默认销毁行为，转而执行 `hide()` 隐藏窗口，并发射 `windowClosed()` 信号通知主窗口。主窗口按钮点击时若窗口已存在则仅调用 `show()`，实现"关闭即隐藏、点击即复现"的无反复创建模式。

3. **数据驱动渲染**：`initEntries()` 构建 7 条 `SynthEntry` 结构体列表，每条记录包含装备中文名、两种合成材料、效果描述、装备类型标签和类型对应颜色。7 种高级装备按类型分为：攻击装（符文大剑/极速战刀）、防御装（反伤铠甲/生命重甲）、攻速装（疾风手套）、蓝装（复活石）、射程装（狙击弩）。

4. **卡片式布局**：`paintEvent()` 中自定义 `QPainter` 绘制全套 UI：
   - 顶部标题 + 副标题 + 分割线
   - 基础装备图例行（5 种基础装备的图标与效果）
   - 合成卡片区：2 列网格布局，每张卡片由 `drawEntry()` 独立绘制，包含左侧类型色条、金色装备名、材料 `A + B` 公式、黄色 `→` 箭头、花费 300 金币标注、绿色效果描述

5. **完全自渲染**：窗口拥有独立的 `paintEvent`，不依赖主窗口的 `update()` 循环，与主战斗渲染完全解耦。主窗口仅负责在析构函数中 `delete m_equipSynthWindow` 释放内存。

#### 4. 独档存档机制

**运作原理**：存档系统使用 **JSON 格式** (`savegame.json`) 实现游戏状态的完整持久化，支持 F5 存档、F9 读档，核心流程如下：

1. **存档触发条件**：仅在 `GamePhase::Preparation`（准备阶段）且 `!m_gameOver` 时允许存档/读档，防止在战斗中保存不一致的中间状态。

2. **保存流程** (`saveGame`)：
   - 构建 `QJsonObject` 根节点，逐项序列化全局状态：`gold`、`currentLevel`、`playerHp`、`pendingGold`、`populationCap`
   - **招募区序列化**：遍历 5 个 `m_recruitSlots`，每槽保存 `type`（职业枚举值）、`price`（波动价格）、`empty`（是否空槽）
   - **棋盘英雄序列化**：遍历 8×8 棋盘，对每个存活的玩家英雄保存：位置 `(x,y)`、类型、星等 `star`、当前 `hp`、最大 `maxHp`、法力值、燃烧剩余回合、**5 槽装备**（按 `EquipType` 枚举顺序，用装备名称字符串标识，空槽存 null）
   - **回收槽序列化**：遍历 16 个回收槽，每槽保存槽位索引 `slot` + 同上完整英雄数据（类型/星等/HP/法力/燃烧/装备）
   - **装备掉落区序列化**：遍历 `m_equipDrops`，用装备名称字符串保存（空位存 null），含 `equipDropCount` 计数
   - 写入前先删除旧存档文件（`file.remove()`），确保覆盖而非追加

3. **读取流程** (`loadGame`)：
   - 打开 `savegame.json`，解析根 JSON 对象
   - 调用 `initGame()` **完全重置**当前游戏状态为初始值
   - 逐项恢复全局状态（金币/关卡/HP/人口等）
   - **英雄重建**：通过 `createHeroFromData` lambda 实现通用反序列化：
     1. 根据 `type` 和 `star` 调用 `createUnitFromPool` 创建单位（构造函数自动设定 `m_maxHp`）
     2. 重置法力值，**先恢复装备再设置 HP**（防御装 `equip()` 会触发 HP 加成，后续 `setHp()` 覆写为存档值，避免装备 HP 重复累加）
     3. 遍历装备名称数组，通过 `createWeaponByName` 按名称重建 Weapon 对象并调用 `equip()`
     4. 恢复法力值（逐个 `gainMana()`）、燃烧状态（`applyBurning(turns)`）
   - 恢复棋盘单位（`m_board.placeUnit`）、回收槽单位（含 slot 定位）、装备掉落区
   - 最后调用 `checkAutoStarUp()` 检查回收槽中是否有满足升星条件的英雄
   - 存档文件损坏或不存在时静默失败（`doc.isNull()` 直接 return）

4. **存档覆盖语义**：`F5` 随时可存，旧档被**先删后建**完全覆盖；`F9` 读档后游戏恢复至保存时的准备阶段状态，可重新布阵或直接开战。

#### 5. 拖曳幽灵渲染

**运作原理**：`renderDragGhost` 负责在拖拽过程中于鼠标光标位置绘制一个半透明的"幽灵"预览，让玩家直观看到正在拖动的单位或装备。该函数在 `paintEvent` 最末层（棋盘→回收槽→单位→信息面板→招募区→装备掉落区→**拖曳幽灵**→右侧UI→羁绊预览）调用，确保幽灵始终渲染在所有棋盘元素之上。

1. **单位拖曳幽灵**（`m_draggedUnit != nullptr`）：
   - 以 `m_dragCurrentPos`（鼠标实时位置）为中心，绘制 `CELL_SIZE × CELL_SIZE` 的圆角矩形
   - **透明度 0.75**：通过 `QPainter::setOpacity(0.75)` 实现半透明效果，使幽灵不会完全遮挡其下方的棋盘格和单位
   - 填充色由 `typeFillColor(type, isHero)` 决定，复用与棋盘单位相同的职业配色方案（战士橙/法师紫/辅助绿/刺客黄），敌方版本颜色更暗
   - 边框使用亮黄色（`QColor(255, 255, 100)`），宽度 2px，使幽灵在深色背景上始终醒目
   - 居中绘制单字中文职业标签（战/法/辅/刺），白色粗体 18px，由 `typeLabel()` 返回

2. **装备拖曳幽灵**（`m_draggedWeapon != nullptr`）：
   - 绘制 48×28 的紧凑圆角矩形（比单位幽灵小，匹配装备卡片的视觉比例）
   - **透明度 0.80**：比单位幽灵略不透明，因装备卡片面积更小且需突出文字可读性
   - 填充深紫灰色（`QColor(45, 40, 60)`），边框金色（`QColor(255, 210, 50)`），与掉落区装备卡片风格一致
   - 居中显示装备展示名称（如"符文大剑"），金色粗体 10px

3. **位置实时跟踪**：`m_dragCurrentPos` 在 `mouseMoveEvent` 中每帧更新为 `event->pos()`，并调用 `update()` 触发重绘。拖拽起始时（`mousePressEvent`）将该点初始化为鼠标按下位置，确保幽灵从拖拽第一帧就出现在正确位置，无跳变。

4. **save/restore 配对**：每次绘制前调用 `painter.save()` 保存画笔状态，绘制完成后 `painter.restore()` 恢复，确保半透明设置和颜色修改不影响后续渲染（右侧 UI、羁绊预览等）。

5. **两种幽灵互斥**：单位和装备不会同时被拖拽，但函数通过两个独立的 `if` 分支而非 `else if` 处理，逻辑上防御了同时非空的异常情况。

#### 6. 拖曳时棋盘格子高亮

**运作原理**：当玩家在准备阶段拖拽英雄时，`renderBoard` 实时检测鼠标下方玩家半场的每个空格子，将幽灵覆盖面积超过一半的格子高亮为绿色，提供直观的"可放置"视觉反馈。放置判定在 `mouseReleaseEvent` 中使用**完全相同的面积重叠算法**选择目标格，确保预览高亮与最终落点严格一致。

1. **触发条件**：三个条件同时满足才进入高亮判定：① `m_draggedUnit != nullptr`（有单位正被拖拽）② `m_phase == GamePhase::Preparation`（处于准备阶段）③ `m_board.isPlayerHalf(y)`（该行位于玩家半场 y≥4）。敌方半场和战斗阶段不触发高亮。

2. **幽灵矩形构造**：以 `m_dragCurrentPos`（鼠标实时坐标）为中心，构造 `CELL_SIZE × CELL_SIZE` 的虚拟矩形，与 `renderDragGhost` 中绘制幽灵的单位矩形尺寸完全一致，确保视觉幽灵与检测区域精确对齐。

3. **重叠面积计算**：对玩家半场的每个格子 `(x, y)`，计算其棋盘矩形 `cellRect(x, y)` 与幽灵矩形的交集：
   ```cpp
   QRect inter = cellRect.intersected(unitRect);
   int overlap = inter.width() * inter.height();  // 交集面积（像素²）
   ```
   高亮条件：`overlap > CELL_SIZE² / 2`（交集面积超过格子面积 50%）且该格未被占用（`!m_board.isOccupied(x, y)`）。满足条件时格子从默认暗蓝色填充切换为 `QColor(50, 90, 50)`（暗绿色）。

4. **放置时的一致性**：`mouseReleaseEvent` 中释放拖拽到棋盘时，遍历玩家半场所有格子执行**相同算法**——构造 `cellRect(x, y)` 与幽灵矩形求交集面积，选择 `overlap > unitArea / 2` 且 `overlap` 最大的格子作为落点。这保证了玩家看到的绿色高亮格子就是松手后英雄最终放置的位置，消除视觉与实际的不一致。

5. **格子填充色**：
   | 状态 | 颜色值 | 视觉效果 |
   |------|--------|---------|
   | 玩家半场（默认） | `QColor(40, 55, 80)` | 暗蓝色 |
   | 敌方半场（默认） | `QColor(75, 38, 38)` | 暗红色 |
   | 拖拽悬停高亮 | `QColor(50, 90, 50)` | 暗绿色 |
   | 格子边框 | `QColor(75, 75, 95)` | 灰蓝色，1px |

6. **装备拖曳不触发**：装备拖曳（`m_draggedWeapon`）不会触发棋盘格子高亮，因为装备只能拖放到英雄身上或掉落区，无需棋盘格级预览。

---

## 更新日志

### v0.20 — 局域网联机对战 + 分模式存档

- **联机对战**：开始界面第四模式。大厅创建房间/输入 IP 加入（QTcpServer/QTcpSocket，端口 45454），
  自动显示本机局域网 IP
- **锁步确定性同步**：双端从阵容快照重建完全相同的棋盘（主机=Hero 侧、客户端=180° 镜像敌方侧）
  + 同种子 + 战斗期禁用随机调用（不掉装备）+ 纯帧驱动逻辑 → 双端逐帧一致；主机补发权威结果对拍
- **对战流程**：3000 金币预算准备 → 双方就绪自动开战 → 战后统计 + 比分计分 → 自动回准备阶段再战
- **分模式存档**：campaign/endless/custom 各一档（自动覆盖），无尽档含波次/强化/编成；
  兼容 legacy savegame.json；联机模式不存档
- Unit 新增 setMaxHp（联机阵容恢复）；Unit::equip 装备由 m_pvpWeapons 持有防泄漏
- 验证：双进程回环对拍（主机强/客户端强两方向）双端 hostWon 与比分镜像一致；
  SendKeys 实测 F5 分模式落档

### v0.19 — 开始界面 + 无尽模式 + 战后统计

- **开始界面**：启动先选模式（通关/无尽/自定义），主游戏页面复用；R 键随时返回重选；显示无尽最佳波数
- **无尽模式**：波次无限。编成演化三段规则——未达 32 上限时随机职业增员；达上限后随机最低星怪物升星；全满星后每波全体 +1% 攻击/生命（无限叠加）。失败即结束并记录最佳波数
- **战后统计面板**：三种模式战斗结束自动弹出，展示每个英雄的输出/承受/治疗/击杀与合计（含阵亡英雄），按输出降序
- 战斗统计归因：普攻/技能伤害与击杀归施法者，承受伤害在 takeDamage 统一累计（含燃烧/反伤）
- 修复：后台进程弹出的新窗口会被 Windows 最小化到任务栏，统计/开始界面改用 showNormal 强制还原
- 演示钩子：SYNERA_AUTOMODE=campaign/endless/custom 跳过开始界面；DEMO_BATTLE=2 仅英雄方（快速验证结算）

### v0.18 — 装备图标修复 + 节奏放慢

- **修复装备图标不显示**：resources.qrc 的 `<file>` 路径带 `src/unit/` 目录层级，
  拼接 prefix 后实际虚拟路径为 `:/equip/src/unit/xxx.png`，而代码加载 `:/equip/xxx.png`
  导致所有图标加载失败（既无图也无字）。为全部 13 项资源添加 `alias` 拍平路径，代码零改动修复
- **放慢整体节奏**：游戏定时器 16ms → 20ms/帧（约 25% 减速），攻击/移动/特效更清晰可见
- **攻击特效加长**：战士斩击 14→22 帧、技能重斩 20→30、刺客 10→16、治疗 30→38、残影 18→26
- **火球飞行显著变慢**：`8+3×距离` → `18+6×距离` 帧（4 格射程约 0.9 秒飞行）
- 演示钩子增强：`SYNERA_DEMO_BATTLE=1` 直接开战、演示英雄自带装备便于验证图标

### v0.17 — 立绘覆盖全部展示位

- 英雄信息面板 / 招募区 / 回收槽（备战区）/ 自定义难度窗口类型按钮全部改用像素立绘，与棋盘风格统一
- 立绘加载与图块绘制抽取为共享 `unitPortrait()`（惰性加载 + 全局缓存）与 `drawUnitChip()`（暗底 + 阵营描边 + 回退色块），主窗口与独立窗口共用一份
- 移除 Synera 内的 m_unitPortraits 数组与加载函数，避免两套加载逻辑

### v0.16 — 全员像素立绘

- **四职业立绘**：战士/法师/辅助/刺客全部使用 `src/unit` 像素立绘替代色块 + 单字标签（Pirate/Witch/GreenGoo/Bird，与 Boss 的 WailingPrince 同套素材）
- 立绘渲染统一：暗色底框 + 阵营描边（英雄蓝/敌方红/Boss 金），名字与 HP/法力条叠加显示
- 拖拽幽灵同步使用立绘预览
- 加载失败自动回退原色块样式；`SYNERA_DEMO_UNITS=1` 验证钩子可在启动时摆出全职业演示阵容用于检查


### v0.15 — 自定义难度系统 + 框架优化

- **自定义难度窗口**：新增独立非模态窗口（与装备合成树同模式），可配置最多 8 组敌方（类型/星级/数量，含 Boss）
- **数量双重校验**：窗口侧总量锁定 ≤ 32（敌方半场容量），主窗口侧对跨窗口数据防御性再校验
- **自定义战斗结算**：独立于关卡进度——不影响玩家 HP、不推进关卡、无利息/通关奖励，仅结算击杀金币；存活英雄照常回收
- **框架优化**：
  - `unitvisuals.h`：职业配色/单字标签/英文名抽取为共享内联定义，主窗口与独立窗口共用，并补充 Boss 配色
  - `isHeroSide()/isEnemySide()`：替代散落全文件的 40+ 处 `dynamic_cast` 阵营判断样板
  - `placeEnemyRandom()`：从 `startBattle` 的 lambda 抽取为成员函数，关卡与自定义战斗共用
  - 开启 `-Wall -Wextra` 并清零全部编译警告（补 Boss 分支、未用参数/变量清理）
- **检测与校验**：
  - 18 项 headless 交互测试（合成鼠标事件驱动真实控件）全部通过
  - `SYNERA_SHOW_CUSTOM=1` 环境变量验证钩子 + 截图视觉验证主窗口按钮与自定义窗口渲染
- 视觉：自定义战斗中关卡标题附加 `(自定义)` 标记；自定义难度按钮战斗阶段置灰


### v0.14 — Boss 立绘 + 全职业战斗打击特效

- **Boss 立绘**：Boss 单位使用 `src/unit/World01_004_WailingPrince.png` 图像展示（暗底金边 + 平滑缩放），替代原来的灰色色块
- **战士普攻特效**：攻击者与目标之间出现刀光弧 + 命中闪光 + 火花溅射，交替挥砍方向
- **战士技能特效**：交叉重斩（双弧线 + 冲击环 + 8 道火花），视觉强度明显高于普攻
- **法师普攻特效**：小火球飞行弹道，沿弧线飞向目标并带拖尾火点，命中后爆裂出冲击环与碎火星
- **燃烧特效**：处于燃烧状态的角色身上持续冒红色火苗（循环上浮 + 摆动 + 淡出）
- **辅助普攻特效**：被治疗者身上冒绿色 "+"，上浮淡出并伴随 2 个小 "+" 飘散
- **辅助技能特效**：大 "+"（22px）伴随 3 个小 "+" 粒子群，覆盖所有被治疗友方
- **刺客普攻特效**：10 帧快速斩击，金白色锋锐刀光（与战士斩击同框架）
- **刺客技能残影**：瞬移路径上留下 3 个高透明度幻影 + 虚线轨迹，快速淡出
- **构建改进**：CMake 优先使用 Qt 自带 MinGW 工具链（不存在时回退 PATH），并把 `src/unit` 图片自动拷贝到构建目录
- 特效全部为纯视觉层，不影响战斗数值；粒子使用确定性伪随机，渲染稳定无闪烁

### v0.13 — 高级装备合成 + 复活机制 + 装备系统扩展

- **高级装备系统**：新增 7 种高级装备（符文大剑/极速战刀/反伤铠甲/生命重甲/疾风手套/复活石/狙击弩）
- **装备合成**：将基础装备拖拽到已装备兼容武器的英雄身上或掉落区另一件装备上，消耗 300 金币自动合成
- **装备栏合成**：支持在掉落区直接拖拽装备到另一件装备上合成（如甲+甲→生命重甲）
- **装备合成树窗口**：新增独立非模态窗口，展示所有合成配方和效果，不影响游戏运行
- **反伤机制**：反伤铠甲装备者受到伤害时反弹 50% 伤害给攻击来源
- **防御机制**：生命重甲提供 20 点护甲减伤（每次受伤 -20，最低减为 0）
- **复活机制**：复活石装备者阵亡时消耗自身，保留其他装备和星级，满血复活到我方半场（优先底排）
- **治疗增幅**：符文大剑为辅助英雄提供 +50 治疗；生命重甲提高 50% 被治疗效果
- **法力回复增幅**：复活石提供双倍法力回复速度
- **装备栏扩展**：掉落区容量 8 → 10，去掉每关掉落上限
- **掉落率提升**：掉落概率 +5%（`关卡×10%` → `关卡×10%+5%`）
- **装备 UI**：装备栏下方新增合成花费提示

### v0.12 — 整星制改革 + 装备系统 + 移动优化

- **整星制改革**：移除半星制，改为纯整星 0-3 星，3 个同名同星英雄自动合成升 1 星
- **装备系统**：新增 5 种装备（剑/甲/攻速/魔法石/战马），战斗中击杀掉落，拖拽装备
- **移动优先级**：从「先横后纵」改为「先纵后横」，修复双方错位时镜像循环卡死
- **初始人口**：5 → 4（首次升级免费）
- **羁绊系统完善**：分身星数适配整星制（`max(0, 星等-1)`）、分身继承已激活全体羁绊、分身可参与羁绊触发判定

### v0.11 — 羁绊系统 + 刺客分身机制修复

- **羁绊系统**：新增 5 种阵容羁绊（战斗不息/吟咏魔典/生生不息/暗夜幻影/全军出击），每帧战斗开始时自动判定激活/失效
- **刺客分身（暗夜幻影）**：2 刺客激活，每个本体生成 1 个分身
- **分身星数修复**：`max(0, starLevel - 1)`（比本体低 1 星，最低 0 星）
- **分身 buff 继承**：分身创建时自动继承已激活的全体羁绊效果（如全军出击）
- **分身羁绊参与**：分身可计入全军出击等全体羁绊的条件判定
- **分身特性**：初始法力 0、本体死亡不影响分身、关卡结束自动移除、不计入人口上限

### v0.10 — 招募系统大改 + 敌方属性同步

- **招募系统重构**：左侧分为英雄信息面板（只读）+ 招募区（5 槽 + 刷新按钮）+ 人口按钮
- **英雄信息面板**：显示 4 种英雄的基础属性（HP/ATK/Heal/射程/移速/攻速/价格），拖拽英雄到对应面板可卖回
- **招募区**：5 个随机英雄槽，点击直接购买，英雄进入回收槽暂存，购买后该槽变空
- **刷新按钮**：花费 15 金币重随全部 5 个招募槽
- **回收槽双用途**：战后回收 + 购买暂存空间
- **敌方属性同步**：刺客敌方 HP 40 → 15，与玩家方完全一致
- 移除旧的商店购买/库存机制，简化拖拽流程

### v0.9 — 半星升星制 + 人口上限 + 金币利息 + 价格波动

- **半星升星制**：星等改为 0-6 半星制，每合成一次 +0.5 星，3 个同整星英雄 → 1 整星
- **升星条件放宽**：按 `starLevel/2`（整星向下取整）匹配，同类型同整星即可合成
- **属性向下取整**：半星无属性加成，仅整星提升 HP/ATK/治疗/技能伤害
- **星星显示**：支持空星 / 左半实心（半星）/ 全实心（整星）三种状态
- **法师技能**：双方法师统一为以自身为中心 5×5 范围（敌方原为 3×3）
- **辅助治疗范围**：1 → 2
- **人口上限系统**：初始 5，升级费 `100×(上限-4)`，UI 显示当前/上限人口
- **金币利息**：胜利后未使用金币 ×1.5（仅在有下一关时生效）
- **价格波动**：每次准备阶段和购买后价格随机波动 ±20，商店实时显示
- **Bug 修复**：升星不再要求精确星等匹配

### v0.8 — 刺客技能锁定 + 伤害显示优化 + 回收槽扩容

- **技能目标验证**：所有角色使用技能前检查有效目标，无目标则保留法力值
- **刺客锁定系统**：刺客技能在燃烧结算后独立处理，支持互杀判定和重索敌
- **寻路优化**：索敌改用欧几里得距离 + 横向距离破平，攻击/辅助统一
- **伤害显示**：浮动 "+X"/"-X" 文本，动态显示时长，回合结束自动清空
- **回收槽扩容**：2×4 → 2×8（16槽），回收时按星数→类型优先级排序
- **帧率翻倍**：全部移速/攻速/燃烧间隔翻倍（优化显示过快问题）
- **法力值保留**：关卡结束后英雄不清空技能点
- **存档覆盖**：保存时显式删除旧存档再建新档
- **Boss 法力修复**：技能检查改用 `getMaxMana()` 替代硬编码值

### v0.7 — 帧级独立计时器 + 关卡/经济系统

- 帧级独立计时器系统取代全局 tick
- 5 关递增难度 + 玩家 HP 系统
- 金币商店 + 击杀奖励经济系统
- 回收槽系统（战后回收英雄）
- Boss 双技能条
- 存档/读档（JSON 格式）
- 升星合成机制

### v0.6 — 法力值与技能系统

- 法力值系统（普攻回蓝，满蓝放技能）
- 四职业技能多态实现
- 燃烧 debuff 系统
- 法力值 UI 指示器

### v0.5 — 三职业 + 自动战斗

- 战士/法师/辅助三种职业类层次
- 全自动战斗系统
- 索敌/移动/攻击/治疗逻辑
- 左侧备战池 + 拖拽布阵
